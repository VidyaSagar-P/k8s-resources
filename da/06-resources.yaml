apiVersion: v1
kind: Pod
metadata:
  name: resources
  labels: 
    project: practice

spec:
  containers: 
    - name: nginx-resource-limit 
      image: nginx
      resources:
        requests:
          memory: "64Mi"
          cpu: "250m"
        limits:
          memory: "128Mi"
          cpu: "500m"
